services:
  redis:
    image: redis:7-alpine
    container_name: osca-backend-redis
    restart: always
    ports:
      - "6379:6379"

  osca-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: osca-backend
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - redis
    develop:
      watch:
        - action: sync
          path: ./src
          target: /usr/src/app/src
        - action: rebuild
          path: package.json
