export const metadata = {
  title: "Developer Dashboard | OSCA",
  description: "Track your contributions, skills, and recommendations",
};

export default function DeveloperDashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
